/**
 * @description: 天气抽屉显示类型
 */
export interface CityDrawerInfo {
  name: string // 城市名称
  province: string // 省
  temp: number // 温度
  weather: string // 天气状况
  lat: number // 纬度
  lon: number // 经度
}
